{
  "name": "seafile-js",
  "version": "0.2.75",
  "private": false,
  "main": "lib/seafile-api.js",
  "dependencies": {
    "axios": "^0.18.0",
    "form-data": "^2.3.2",
    "package.json": "^2.0.1"
  },
  "scripts": {
    "prepublish": "node_modules/babel-cli/bin/babel.js src --out-dir lib",
    "test": "jest --env=node --colors --coverage",
    "build:src": "babel ./src --out-dir ./lib --delete-dir-on-start --copy-files",
    "build": "NODE_ENV=production yarn build:src",
    "dev": "NODE_ENV=development yarn build:src --watch --source-maps"
  },
  "jest": {},
  "babel": {
    "presets": [
      "es2015"
    ]
  },
  "devDependencies": {
    "@babel/cli": "^7.1.2",
    "@babel/core": "^7.1.2",
    "babel-preset-react-app": "^5.0.3",
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "eslint": "^4.19.1",
    "jest": "^23.4.2",
    "prepublish": "^2.1.2"
  }
}
